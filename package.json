{
  "name": "temmulex",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --import dotenv/config server.js",
    "local": "node --import dotenv/config --watch --env-file=.env.local server.js",
    "dev": "node --import dotenv/config --env-file=.env.development server.js",
    "production": "node --import dotenv/config --env-file=.env.production server.js",
    "drizzle:push": "npx drizzle-kit push",
    "drizzle:migrate": "npx drizzle-kit migrate",
    "drizzle:generate": "npx drizzle-kit generate",
    "drizzle:studio": "npx drizzle-kit studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@socket.io/redis-adapter": "^8.3.0",
    "@vinejs/vine": "^4.1.0",
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.8.0",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "express": "^5.1.0",
    "ioredis": "^5.8.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.10",
    "pg": "^8.16.3",
    "socket.io": "^4.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.7"
  }
}
